# Env
.env
.env.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Dev PWA build artefakt (genereras av vite-plugin-pwa i dev)
dev-dist/

# Lokal Claude Code-config (maskinspecifik)
.claude/settings.local.json

# Pose model — binary asset fetched on demand via `npm run pose:model`
public/models/*.task

# Pose WASM runtime — copied from node_modules via `npm run pose:wasm`
public/wasm/

# Offline Vision evaluation (scripts/vision-eval.mjs): source clips, extracted frames and
# raw model responses. Erik's own recordings plus a few hundred JPEGs and MBs of JSON —
# the generated report in docs/experiments/ is the artefact worth keeping.
# Root-anchored: an unanchored `experiments/` would also swallow docs/experiments/.
/experiments/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
